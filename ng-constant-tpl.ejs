(function (angular) {
  'use strict';

  angular.module('{%- moduleName %}'{% if (deps) { %}, {%= JSON.stringify(deps) %}{% } %})
  {% constants.forEach(function (constant) { %}.constant('{%- constant.name %}', {%= constant.value.replace(/\"/g, '\'') %})
  {% }); values.forEach(function (value) { %}.value('{%- value.name %}', {%= value.value %}){% }) %};

})(window.angular);
